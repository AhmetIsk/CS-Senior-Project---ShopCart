import styled from 'styled-components';
import { colors } from '../../constants/styles';

export const MyShoppingList = styled.Text`
  color: ${colors.headerRed};
  font-weight: 700;
  font-size: 24px;
`;
